# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Secrets — never commit
.env
*.pem
*.key

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Evaluation harness — internal only, never commit
# E2E test apps live in C:\Users\haris\AWS-CDK (separate repo)
tests/e2e/
tests/harness/

# Local session files
CLAUDE.md
.coverage
coverage.xml

# Demo recording — tape file is internal, gif gets committed after recording
demo.tape

# Internal docs — architecture/paper material, never commit
docs/internal/
